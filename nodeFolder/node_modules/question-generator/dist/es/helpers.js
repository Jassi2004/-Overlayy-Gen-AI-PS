var sanitize = function sanitize(input) {
  var result = input;
  result = toLowerCase(result);
  result = trimString(result);

  return result;
};

export var toLowerCase = function toLowerCase(input) {
  return input.toLowerCase();
};
export var trimString = function trimString(input) {
  return input.trim();
};

export default sanitize;